#include <stdio.h>
#include <stdlib.h>


int main (){
    float x = 10.000;
    float y = 20.000;
    if (x > y)
        return 1;
    if (x == y)
        return 0;
    if (x < y)
        return -1;   
}